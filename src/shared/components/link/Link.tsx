import { Button } from '@mui/material';
import styled from 'styled-components';

const InputLogin = styled(Button)`
    &.MuiButton-root{
        color: white;
        
    }
    text-decoration: none;
    &:hover{
        color: yellow;
    }

`; 
export default InputLogin;